<header class="header">
  <nav class="navbar navbar-expand-lg " style="background-color: #4285f4;">
    <div class="container">
        <div class="logo">
            <a href="/">
                <img src="/public/img/logo.png">
            </a>
        </div>
        <!-- desplegable -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <a href="/contact" class="btn order-first order-lg-last"><button>Contact</button></a>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <!-- lista -->
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if (typeof usuario === 'undefined') { %>
            <li class="nav-item">
                <a class="nav-link" href="/logIn">Iniciar Sesión</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/signUp">Crear Cuenta</a>
            </li>
            <% } %>
            <% if (typeof usuario !== 'undefined') { %>
              <li class="nav-item">
                  <a class="nav-link" href="/forum">Muro</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/logout">LogOut</a>
              </li>
            <% } %>
        </ul>
      </div>
    </div>
  </nav>
</header>